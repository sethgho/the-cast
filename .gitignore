# Build output — regenerated by scripts/build-site.py / the Docker build.
# Anchored to the repo root on purpose: an unanchored `public/` also swallowed
# local-comfy/celld-editor/public/, which is hand-written source, not build output.
/public/

.DS_Store
__pycache__/
*.pyc

# dev-time matting venv — rembg + a 973 MB ONNX model, never a build dependency
.venv-matte/
